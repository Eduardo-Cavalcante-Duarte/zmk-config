#include <dt-bindings/zmk/matrix_transform.h>
#include "la_usurpadora-layouts.dtsi"

/ {

    none: matrix_transform {
        compatible = "zmk,matrix-transform";
        columns = <0>;
        rows = <0>;
        map = <>;
    };

    chosen {
        zmk,matrix-transform = &none;
    };
};
